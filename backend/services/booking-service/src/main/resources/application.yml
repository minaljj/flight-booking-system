spring:
  application:
    name: booking-service
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://localhost:8888/}"
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 4000
        max-attempts: 5